import 'package:flutter/cupertino.dart';

class Schedule extends StatelessWidget {
  final VoidCallback onTap;

  const Schedule({required this.onTap});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(

      ),
    );
  }
}
